{
  "name": "public-standards-wg",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "scripts": {
    "check:imports": "node scripts/check-imports.mjs",
    "audit:consistency": "node scripts/audit-consistency.mjs",
    "audit:consistency:strict": "node scripts/audit-consistency.mjs --strict",
    "audit:all": "node run-audit-all.mjs",
    "test:example": "npm run check:imports --prefix examples/basic-app && npm run audit:consistency --prefix examples/basic-app && npm run audit:consistency:strict --prefix examples/basic-app",
    "test:agent-coordination": "node --test scripts/agent-coordination/*.test.mjs",
    "test:schema-usage-guard": "node --test scripts/schema-usage-guard/*.test.mjs",
    "ci": "npm run audit:all && npm run test:agent-coordination && npm run test:schema-usage-guard"
  }
}
